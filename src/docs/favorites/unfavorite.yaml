Endpoint for a logged in user to unfavorite a favorited item
---
tags:
  - Favorites
security:
  - Authorization: []
parameters:
  - in: path
    name: id
    required: true

responses:
  204:
    description: Unfavorited

  404:
    description: Item not found