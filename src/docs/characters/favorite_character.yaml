Endpoint for a logged in user to favorite a character
---
tags:
  - Characters
security:
- Authorization: []
parameters:
  - in: path
    name: id
    required: true

responses:
  201:
    description: Character saved as favorite

  500:
    description: Internal server error